
// const number = parseInt(prompt('enter an interger: '));
//     for (let a = 1; a <= 12; a++) {
//         const result = a * number;
//     }
//         console.log(`${number} * ${a} = ${result}`);   


    // for (let a = 2; a < 24; a   ) 
    //     console.log(a)


     function timetable(number) {
         for (let a = 1; a <= 12; a++) {
             let total = number * a
            console.log(`${number} * ${a} = ${total} `);        
         }
 } 
    timetable(100)

    // function timetable(number, Range) {
    //     for (let a = 1; a <= Range; a++) {
    //         let total = number * a
    //         console.log(`${number} * ${a} = ${total} `);        
    //     }
    // }
    // timetable('3', '6')